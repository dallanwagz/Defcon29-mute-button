"""Archived PageDefs — kept for reference but not loaded by the bridge runtime.

Modules here are not imported by :mod:`dc29.bridges.registry`. To re-enable an
archived page, copy its PageDef back into ``registry.py`` and add it to
``ALL_PAGES``.
"""
